# Generated by scripts/copy-registry.js at prestart (do not commit).
# These mirror the canonical sources in `../registry/`. Hand-written
# website-chrome components live in `app/_components/` (the underscore-
# prefix folder convention puts them outside the router's reach AND
# outside this mirror's scope), so this directory can stay wholly
# ignored without any `!` exceptions.
/components/
/lib/

# Generated by Tailwind CSS build (do not commit).
public/tailwind.css
