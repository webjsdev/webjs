# Generated by scripts/copy-registry.js at prestart (do not commit).
# These mirror the canonical sources in `../registry/`. Use `/dir` form
# so only the top-level dirs match — not nested ones like `app/docs/components/`.
/components/
/lib/

# Generated by Tailwind CSS build (do not commit).
public/tailwind.css
