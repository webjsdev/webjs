# Generated by scripts/copy-registry.js at prestart (do not commit).
# These mirror the canonical sources in `../registry/`. The `/dir/*` form
# (rather than `/dir/`) ignores only the contents — gitignore can't re-
# include files inside a wholly-ignored directory, so we ignore contents
# instead and then explicitly allow hand-written exceptions below.
/components/*
/lib/*

# Hand-written files inside /components/ that aren't part of the registry
# mirror — add explicit allows here as needed.
!/components/theme-toggle.ts

# Generated by Tailwind CSS build (do not commit).
public/tailwind.css
